[project]
name = "visual-atlas"
version = "0.1.0"
description = ""
authors = [
    {name = "GuilhermeCF",email = "guicostaferreira.2003@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nibabel (>=5.3.3,<6.0.0)",
    "matplotlib (>=3.10.7,<4.0.0)",
    "tqdm (>=4.67.1,<5.0.0)",
    "scipy (>=1.16.3,<2.0.0)",
    "simpleitk (>=2.5.3,<3.0.0)"
]

[tool.poetry]
packages = [{include = "visual_atlas", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
